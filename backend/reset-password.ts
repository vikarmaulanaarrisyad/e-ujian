const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcrypt'); const prisma = new PrismaClient(); async function main() { const hashedPassword = await bcrypt.hash('123456', 10); await prisma.user.update({ where: { username: 'admin_ikhsaniyah' }, data: { password: hashedPassword } }); console.log('Password reset successfully to 123456'); } main().finally(() => prisma.$disconnect());
